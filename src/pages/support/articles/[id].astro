---
import DocPage from '@/components/support/DocPage.astro';
import { loadDoc } from '@/support/load';

const doc = await loadDoc('articles', Astro.params.id, Astro);
if (!doc) return Astro.redirect('/404');
---

<DocPage doc={doc} />
